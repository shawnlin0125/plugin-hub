apiVersion: v1
kind: ConfigMap
metadata:
  name: vendor-routing
data:
  routing.yaml: |
    ticketmaster:
      target: vendor-heavy-1
      reason: "High volume — dedicated deployment"
    kktix:
      target: vendor-heavy-2
      reason: "High volume — dedicated deployment"
    ibon:
      target: vendor-bundle
    fami:
      target: vendor-bundle
    tixcraft:
      target: vendor-bundle
